# Destino das aplicações do formulário (obrigatório antes de publicar).
# Endpoint que recebe um POST JSON a cada envio — Supabase Edge Function,
# Google Apps Script (planilha), Make/Zapier (WhatsApp/Discord da equipe)…
VITE_FORM_WEBHOOK_URL=
